ArraList = []{Employe1, }
Employee {
int ID,
String Name,
}

HashMap<Integer, Employee> employeeMap = new HashMap<>();
list.forEach(employee -> employeeMap.put(employee.getId(), employee));

Employee Table has ID, Name, Department
Salary Table has ID, Salary

SELECT DEPARTMENT, MAX(SALARY) FROM EMPLOYEE E, SALARY S WHERE E.ID = S.ID Group BY DEPARTMENT